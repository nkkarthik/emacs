#+title: e CLAUDE


* claude

Working log maintained by Claude Code sessions in this repo.  Move
items from /todo/ to /done/ as they complete; record new work at the
top of /done/ (reverse-chronological — newest first).

** todo

** done

- 2026-05-24 =e.mk install-a=: also drop a =site-lisp/= symlink at repo root pointing at =nextstep/Emacs.app/Contents/Resources/site-lisp/= so =src/emacs= (and any daemon started via =brew-bin=/=local-bin=, i.e. =/opt/homebrew/bin/emacs -> src/emacs=) finds the bundled =a/= packages on =load-path=.  Root cause: =src/emacs= computes a bogus relative =Contents/Resources/site-lisp= entry and never resolves the installed bundle; only =Emacs.app/Contents/MacOS/Emacs= (the real Mach-O) finds its own =site-lisp/=.  Also wipe stale =emacs-libvterm/build/= leftovers in the install before rsync so they don't pollute =load-path= via =subdirs.el= discovery.  Verified: =(require 'claude)=/=msmail=/=codex= now succeed under =src/emacs --batch --no-init-file=; daemon will pick this up on next restart.  Updated =CLAUDE.md='s Install-layout section to describe the symlink.
- 2026-05-24 e.org watcher: switch poller's pull to =git pull --rebase --autostash= so a dirty working tree no longer silently blocks remote updates (root cause: previous =[ -z "$(git status --porcelain)" ]= gate skipped pulls indefinitely while =a/= had uncommitted changes from another session).  Re-armed.
- 2026-05-24 Delegated =a/daemon/= portability task from =e.org='s =* claude= to =a/daemon/CLAUDE.org= per the new "Never work on =a/*= directly" rule (task: hardcoded username in =e.plist=/=ec.plist=/=e.service= → templatize at install time).  Removed the line from =e.org=.
- 2026-05-24 =CLAUDE.md=: add strict "Never work on =a/*= directly" section — root-repo session must queue any =a/<package>/= task into =a/<package>/CLAUDE.org= under =* claude= and stop; no edits, builds, or "just look first" passes into package internals.  Also saved as a memory entry.
- 2026-05-24 =CLAUDE.md=: strengthen the watcher rule — never suggest stopping the watcher, never surface it in any "I can stop it if..." form; only stop on unprompted user request, silently with no follow-up.
- 2026-05-24 Switch =CLAUDE.org='s =** done= log to reverse-chronological order (newest at top), reverse the existing entries in place, and update =CLAUDE.md='s working-log section to match (was: "oldest at the top so it reads chronologically" → "newest at the top so the most recent work is what a future session sees first").
- 2026-05-24 Remove =compose.yaml= (it referenced a =Dockerfile= that no longer exists in the tree, so it was orphaned anyway) and drop the paired bullet for =compose.yaml=/=Dockerfile= from =CLAUDE.md='s personal-files list.
- 2026-05-24 =e.mk=: add =vterm-module= target that builds =a/emacs-libvterm/= via CMake and emits =vterm-module.so= next to =vterm.el=.  Make =install-a= depend on it (so the Darwin chain ships both into the bundle's =site-lisp/=) and add it as a =Linux= prereq.  Add =libvterm= to brew =deps= and =libvterm-dev= + =cmake= to apt =ldeps=.  Detect and wipe stale =build/CMakeCache.txt= when its recorded source dir doesn't match this checkout (happens when =build/= was populated from another clone path).  Also exclude =build/= from the =install-a= rsync so cmake intermediates don't land in =site-lisp/=.  Verified end-to-end on Darwin: System libvterm detected, =vterm-module.so= built, second invocation is a cache-friendly no-op.
- 2026-05-24 =e.mk= =brew-check=: stop auto-invoking =brew-install= when Homebrew is missing; instead print a clear error pointing the user at =make -f e.mk brew-install= and exit 1.  =brew-install= now just prints the official one-liner for the user to run knowingly rather than piping =curl | bash= itself.  Verified =brew-check= still passes on this machine and =brew-install= prints the expected instruction.
- 2026-05-23 Rename =a/codel/= → =a/codex/= (=git mv=) so the directory matches the package file =codex.el=.  No build/config refs needed updating (the =install-a= rsync picks up subdirs by their actual names).  Internal =codel= mentions inside the package (=URL:= header in =codex.el=, =/v/codel= test paths, =README.md=) describe the upstream project identity and stay as-is.
- 2026-05-23 Move =e.plist=, =ec.plist=, =e.service= from repo root into =a/daemon/= (=git mv=); update =e.mk= =launch=/=launchr=/=system= targets to the new paths; exclude =daemon/= from the =install-a= rsync so launchd/systemd units stay out of =site-lisp/=; refresh =CLAUDE.md=.  Verified via =make -f e.mk -n launch / launchr / system= and an =rsync -n= dry-run.
- 2026-05-23 Rebuild Emacs from source on macOS (=make -f e.mk build=).  First attempt hit a portable-dumper fingerprint mismatch (=src/emacs.pdmp= stale vs =src/emacs=); =rm src/emacs.pdmp= + rerun recovered.  Verified the installed Emacs 32.0.50 loads all four =a/= packages: =vterm=, =msmail=, =claude=, =codex= (from =site-lisp/codel/codex.el=).
- 2026-05-23 Vendor =a/emacs-libvterm/= (source only — no =build/=, no =.so=) and import =a/codel/= (=codex.el= + tests + design docs) on a feature branch =codex/claude-vterm-command=; merged fast-forward into =master=.
- 2026-05-23 Widen =a/.gitignore= =*.db= → =*.db*= so SQLite WAL side files (=mail.db-shm=, =mail.db-wal=) are also untracked.
- 2026-05-23 Tighten =install-a= excludes (=*.org=, =*~=, =*.db*=, =*.elc=, =*-tests.el=) so only package =.el= source ships into =site-lisp/=; verified =site-lisp/msmail/= contains only =msmail.el= and the installed Emacs loads =msmail= cleanly (=msmail-login=/=msmail-sync=/=msmail= bound).
- 2026-05-23 Merge =a/msmail/{plan,todo,done}.org= into =mail.org= as demoted top-level sections (=* plan=, =* todo=, =* done=); rename to =msmail.org=; delete the source files and =mail.org~= backup.
- 2026-05-23 Verify installed Emacs auto-adds =site-lisp/foo/= to =load-path= via the bundle's =subdirs.el=; =(require 'foo)= resolves and =(foo-hello)= returns the expected string.  Note: =-Q= disables site-lisp processing — use =--no-init-file= alone when verifying =a/= installs.
- 2026-05-23 Add sample =a/foo= package (=foo.el= with =foo-hello=) and =install-a= target in =e.mk= that rsyncs =a/= into =Emacs.app/Contents/Resources/site-lisp/=, hooked into the post-install step.
- 2026-05-23 Add =CLAUDE.md= at repo root and =a/CLAUDE.md= covering build/upstream/install conventions.
